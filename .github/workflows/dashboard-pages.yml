name: Dashboard Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: dashboard-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "11"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build pathfinder dashboard
        run: ./gradlew clean pathfinderDashboard --stacktrace
        shell: bash

      - name: Build clue steps dashboard
        run: ./gradlew reachabilityDashboard -PdashboardDataset=/reachability/clue_locations_full.csv -PdashboardProfile=false --stacktrace
        shell: bash

      - name: Build targets dashboard
        run: ./gradlew reachabilityDashboard -PdashboardDataset=/reachability/targets.csv -PdashboardProfile=false --stacktrace
        shell: bash

      - name: Build quetzal whistle routes dashboard
        run: ./gradlew reachabilityDashboard -PdashboardDataset=/reachability/quetzal_whistle_routes.csv -PdashboardProfile=true --stacktrace
        shell: bash

      - name: Build profiled routes dashboard
        run: ./gradlew reachabilityDashboard -PdashboardDataset=/reachability/routes.csv -PdashboardProfile=true --stacktrace
        shell: bash

      - name: Upload dashboard bundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: pathfinder-dashboard-site
          path: build/reports/pathfinder-dashboard
          if-no-files-found: error

      - name: Configure Pages
        if: github.event_name != 'pull_request'
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/reports/pathfinder-dashboard

  deploy:
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
